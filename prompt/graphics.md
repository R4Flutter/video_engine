# GRAPHIC PROMPTS — MOTION GRAPHICS (build in vox engine)

These are engine modules, not images. All use the vox font + cream/ink palette.

---

**g1 — stamp_slam** (0:08, "$180 — 1935")
> Rubber stamp "SLAMS" onto the receipt: scale 1.6→1 with 0.2s, slight rotation
> 6deg→0, impact shake 2 frames, ink texture dries in. Text: "$180 — 1935",
> charcoal ink on cream. Sound: thud. (engine: stat/slam)

**g2 — number_ticker** (0:25, "$180 → $1.8M")
> Rolling-digit counter: digits tumble from top, $180 counts up to $1,800,000
> over 2.5s, one digit at a time, then "→ $1.8M" compact label pops. Red accent
> on the final "M". Sound: rolling digits. (engine: stat with tween)

**g3 — share_split** (0:27, 1→2→4→8)
> Single share card splits in half, halves separate and slide apart, each splits
> again — 4 splits in 2.5s, cards stack in a grid. Paper-shuffle sound per split.
> (engine: icon/callout stagger)

**g4 — timeline** (0:05 optional, 1935→2010)
> Horizontal axis draws across the page: "1935" dot lands (the purchase), long
> empty run to "2010" dot (the death), one label hangs below the axis: "DID
> NOTHING". (engine: timeline module — same one-dot pattern as the Kodak film
> video's beat 2)

**g5 — contrast_flip** (0:05, salary slip vs roulette wheel)
> Split screen: left = salary slip card, right = roulette wheel card, both
> paper-cut. Cards flip in from opposite edges with a 2-frame stagger, keyword
> chips "SALARY?" / "LUCKY TRADE?" pop above each. (engine: compare module)

**g6 — highlighter_sweep** (0:55, "DOING NOTHING. CORRECTLY.")
> A highlighter line sweeps left→right across the payoff line in 0.6s, then
> holds. Yellow marker on cream paper. Sound: pen sweep. (engine: kinetic +
> motion FX — same sweep as Kodak beat 7)

**g7 — digit_roll** (0:48, "$7,000,000")
> Full-frame number rolls in digit by digit over 2s, ink dry-brush texture,
> scales up slightly with the reveal. Sound: vault latch then coins. (engine:
> stat)

---

**Rule:** one graphic idea per scene. If the scene already has an illustration
(artifact), the graphic carries the NUMBER only.