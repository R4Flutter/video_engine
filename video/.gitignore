node_modules
dist
.DS_Store
.env

# Render outputs / local caches
out
cache
.tmp

# Derived local narration artifacts
public/audio/vo/.takes.json
public/audio/vo/.heard.json

# External/legacy media fetch folders are not part of the production asset library
public/footage

# Production assets are intentionally tracked when supplied/generated for an episode.
# Put daily inputs only in public/assets/00_INBOX before organizing them.
