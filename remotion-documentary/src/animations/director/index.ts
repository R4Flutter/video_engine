// Director — the AI-callable surface for the whole library.
// Pick an effect by name; the Director looks it up in REGISTRY and renders it.

export { Director } from "./Director";
export { REGISTRY } from "./registry";
export { Placeholder } from "./Placeholder";
