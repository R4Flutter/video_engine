# The Chatterbox venv:  py -3.11 -m venv .venv-tts
#                       .venv-tts/Scripts/pip install -r tools/requirements-tts.txt
# Kept apart from the main interpreter because chatterbox pins torch==2.6.0.
chatterbox-tts==0.1.7
# chatterbox's watermarker (resemble-perth) still imports pkg_resources, which
# setuptools dropped in 81. Without this pin it loads as None and TTS dies on
# the first from_pretrained().
setuptools<81
